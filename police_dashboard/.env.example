# Base URL the dashboard uses to reach the AIRA backend.
# In dev, Vite proxies "/api" and "/uploads" to localhost:8000 (see vite.config.ts),
# so a relative path is fine. Override here to hit a remote backend directly.
VITE_API_BASE_URL=/api/v1
VITE_BACKEND_ORIGIN=http://localhost:8000
